@page
@model gestione_magazzino.Pages.magazzino.IndexModel
@{
}

<h2>Visualizza Magazzino</h2>
<a asp-page="./Create">Create New</a>
<p></p>
<div class="row">
    @foreach (var item in @Model.eleMagazzino)
    {
        <div class="card" style="width: 18rem; margin-left:1rem">
            <div class="card-body">
                <h5 class="card-title">nome: @item.nome</h5>
                <h5 class="card-title">indirizzo: @item.indirizzo</h5>
                <h5 class="card-title">numero: @item.numero</h5>
                <h5 class="card-title">ID: @item.id</h5>
            </div>
            <a class="btn-danger" asp-page="./Delete" asp-route-id="@item.id">Elimina</a>
            <a class="btn-dark" asp-page="./Edit" asp-route-id="@item.id">Modifica</a>
        </div>
    }
</div>
